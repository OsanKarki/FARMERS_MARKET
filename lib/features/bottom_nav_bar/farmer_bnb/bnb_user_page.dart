import 'package:flutter/material.dart';

class BnbUserPage extends StatelessWidget {
  const BnbUserPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('user')),

    );
  }
}
